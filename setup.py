from setuptools import setup

setup(
    name='isolate',
    version='0.0.1',
    description='A library to interface with `isolate`',
    author='Andrew Chow',
    author_email='andrew@cho.io',
    license='MIT',
    packages=['isolate'],
)
